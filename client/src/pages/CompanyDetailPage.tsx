import { ArrowLeft, Building2, Gift, Loader2, Mail, Plus, ShieldCheck } from "lucide-react";
import { useEffect, useState } from "react";
import { Link, useLocation, useRoute } from "wouter";
import DashboardLayout from "@/components/DashboardLayout";
import { resolveDashboardDestination } from "@/lib/dashboardAccess";
import { trpc } from "@/lib/trpc";

export default function CompanyDetailPage() {
  const [, params] = useRoute("/company/:id");
  const [, setLocation] = useLocation();
  const companyId = Number(params?.id ?? 0);
  const [contact, setContact] = useState({ label: "Bookings", contactType: "email" as const, value: "", isPublic: true, sortOrder: 0 });
  const [offer, setOffer] = useState({ code: "", title: "", description: "", discountType: "percentage" as const, discountValue: 10 });
  const membership = trpc.companies.membership.useQuery({ companyId }, { enabled: Number.isInteger(companyId) && companyId > 0, retry: false });
  const ownerAccess = membership.data?.memberRole === "owner";
  const contacts = trpc.companies.contacts.useQuery({ companyId }, { enabled: ownerAccess, retry: false });
  const offers = trpc.companies.offers.useQuery({ companyId }, { enabled: ownerAccess, retry: false });
  const replaceContacts = trpc.companies.replaceContacts.useMutation({ onSuccess: () => contacts.refetch() });
  const createOffer = trpc.companies.createOffer.useMutation({ onSuccess: () => { offers.refetch(); setOffer({ code: "", title: "", description: "", discountType: "percentage", discountValue: 10 }); } });
  useEffect(() => { if (membership.isError) setLocation(resolveDashboardDestination(`/company/${companyId}`, "user", false)); }, [membership.isError, companyId, setLocation]);
  if (membership.isLoading) return <DashboardLayout eyebrow="SURA / COMPANY" title="Opening your company studio." description="Checking secure company membership before showing management details."><div className="grid min-h-64 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#a96b35]" /></div></DashboardLayout>;
  if (membership.isError || !membership.data) return <DashboardLayout eyebrow="SURA / COMPANY" title="Returning to your company studio." description="Company management requires an active owner, manager, or editor membership."><div className="rounded-[1.5rem] border border-[#e3c1b2] bg-[#fff3ed] p-7 text-sm text-[#7e4839]">Access was not granted. Redirecting to your company studio…</div></DashboardLayout>;
  return <DashboardLayout eyebrow="SURA / COMPANY MANAGEMENT" title="A studio with secure boundaries." description="Only active company members can open this management space. Owner controls are used for public contacts and offers."><Link href="/company" className="vb-focus inline-flex items-center gap-2 text-sm font-bold text-[#795634]"><ArrowLeft className="h-4 w-4" />Back to all company spaces</Link><section className="mt-7 rounded-[1.6rem] border border-[#ded1bf] bg-[#fbf8f2] p-7"><div className="flex items-start justify-between gap-4"><div><p className="vb-kicker text-[#9d5b2d]">Secure company access</p><h2 className="vb-serif mt-3 text-4xl">Member role: {membership.data.memberRole}</h2><p className="mt-3 max-w-lg text-sm leading-6 text-[#756655]">Company contact details and offers stay private until an owner submits them and platform review allows public display.</p></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-[var(--sura-primary)] text-[var(--sura-accent)]"><ShieldCheck className="h-5 w-5" /></div></div></section>{!ownerAccess ? <section className="mt-6 rounded-[1.5rem] border border-dashed border-[#d2bda9] bg-[#f8f1e6] p-6 text-sm leading-6 text-[#725c47]">You can view this secure studio, but owner-level access is required before public contact details or discount offers can be changed.</section> : <div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="rounded-[1.5rem] border border-[#ded1bf] bg-[#fbf8f2] p-6"><Mail className="h-5 w-5 text-[var(--sura-accent)]" /><h2 className="vb-serif mt-4 text-3xl">Public contact route</h2><p className="mt-2 text-sm leading-6 text-[#756655]">Share only the channel a customer should use. This replaces the current public company contacts.</p><input value={contact.label} onChange={(event) => setContact({ ...contact, label: event.target.value })} className="vb-focus mt-5 w-full rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm" placeholder="Bookings" /><select value={contact.contactType} onChange={(event) => setContact({ ...contact, contactType: event.target.value as typeof contact.contactType })} className="vb-focus mt-3 w-full rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm"><option value="email">Email</option><option value="phone">Phone</option><option value="whatsapp">WhatsApp</option><option value="address">Address</option></select><input value={contact.value} onChange={(event) => setContact({ ...contact, value: event.target.value })} className="vb-focus mt-3 w-full rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm" placeholder="hello@yourstudio.co.ke" /><button onClick={() => replaceContacts.mutate({ companyId, contacts: [contact] })} disabled={!contact.value || replaceContacts.isPending} className="vb-button vb-focus mt-4 rounded-full bg-[var(--sura-primary)] px-5 py-3 text-sm font-bold text-[var(--sura-paper)]">Save contact</button><div className="mt-5 space-y-2">{contacts.data?.map((item) => <p key={item.id} className="rounded-xl border border-[#e5dbce] bg-white p-3 text-sm"><strong>{item.label}:</strong> {item.value}</p>)}</div></section><section className="rounded-[1.5rem] border border-[#ded1bf] bg-[#fbf8f2] p-6"><Gift className="h-5 w-5 text-[var(--sura-accent)]" /><h2 className="vb-serif mt-4 text-3xl">Discount offer</h2><p className="mt-2 text-sm leading-6 text-[#756655]">Offers start as pending and only become public after SURA review.</p><input value={offer.code} onChange={(event) => setOffer({ ...offer, code: event.target.value.toUpperCase() })} className="vb-focus mt-5 w-full rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm" placeholder="WELCOME10" /><input value={offer.title} onChange={(event) => setOffer({ ...offer, title: event.target.value })} className="vb-focus mt-3 w-full rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm" placeholder="A warm first visit" /><textarea value={offer.description} onChange={(event) => setOffer({ ...offer, description: event.target.value })} className="vb-focus mt-3 min-h-20 w-full rounded-xl border border-[#ded1bf] bg-white p-3 text-sm" placeholder="Offer details" /><div className="mt-3 flex gap-3"><select value={offer.discountType} onChange={(event) => setOffer({ ...offer, discountType: event.target.value as typeof offer.discountType })} className="vb-focus flex-1 rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm"><option value="percentage">Percentage</option><option value="fixed_kes">KES amount</option></select><input type="number" min="1" value={offer.discountValue} onChange={(event) => setOffer({ ...offer, discountValue: Number(event.target.value) || 1 })} className="vb-focus w-28 rounded-xl border border-[#ded1bf] bg-white px-3 py-3 text-sm" /></div><button onClick={() => createOffer.mutate({ companyId, ...offer })} disabled={!offer.code || !offer.title || createOffer.isPending} className="vb-button vb-focus mt-4 inline-flex items-center gap-2 rounded-full border border-[var(--sura-border)] px-5 py-3 text-sm font-bold text-[var(--sura-ink)]"><Plus className="h-4 w-4" />Submit offer</button><div className="mt-5 space-y-2">{offers.data?.map((item) => <p key={item.id} className="rounded-xl border border-[#e5dbce] bg-white p-3 text-sm"><strong>{item.code}</strong> · {item.title} <span className="text-xs text-[#796957]">({item.status})</span></p>)}</div></section></div>}</DashboardLayout>;
}
